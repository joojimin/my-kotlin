fun drawCircle() {
    val circle = object {
        var x = 10
        val y = 20
        val radius = 30
    }

    println("Circle x: ${circle.x} y: ${circle.y} radius:${circle.radius}")
}

drawCircle() // Circle x:10 y:20 radius:30